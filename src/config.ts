// Central place for the consultation booking link.
// TODO: replace with the real Acuity Scheduling URL for the first session.
export const BOOKING_URL = "#";

// Production origin — used for canonical URLs, the sitemap, and absolute
// OG/Twitter image URLs. Must match astro.config's `site`. No trailing slash.
export const SITE_URL = "https://interpreterofbiology.com";

// Absolute URL to the social-share image (lives in /public).
export const OG_IMAGE = `${SITE_URL}/og.jpg`;
