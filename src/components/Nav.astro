---
import { BOOKING_URL } from "../config";
const links = [
  { href: "#problem", label: "The problem" },
  { href: "#analyst", label: "The science" },
  { href: "#workflow", label: "The program" },
];
---

<header
  class="sticky top-0 z-50 bg-paper/90 backdrop-blur border-b-2 border-ink"
>
  <nav
    class="mx-auto flex max-w-6xl items-center justify-between px-5 py-3"
    aria-label="Main"
  >
    <a href="#top" class="group leading-none">
      <span class="font-serif italic text-xl text-ink">
        interpreter of biology
      </span>
      <span class="block h-1 w-44 bg-neon-lime"></span>
    </a>
    <div class="hidden items-center gap-7 md:flex">
      {
        links.map((l) => (
          <a href={l.href} class="font-mono text-sm text-ink/80 hover:text-ink">
            {l.label}
          </a>
        ))
      }
    </div>
    <a
      href={BOOKING_URL}
      class="font-sans font-bold text-sm text-white bg-primary border-2 border-ink rounded-full px-4 py-2 hover:-translate-y-0.5 transition-transform"
    >
      Apply for your blueprint
    </a>
  </nav>
</header>
